class Main {

    /** Initializes a new game and starts running it. */
    function void main() {
        var Game game;

        let game = Game.new();
        do game.run();
        
        do game.dispose();
        return;
    }
}